{
  "name": "eventpass-glitch-runner",
  "version": "1.0.0",
  "description": "Root package.json to help Glitch auto-start the backend folder",
  "scripts": {
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
